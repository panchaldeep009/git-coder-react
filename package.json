{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "build": "yarn lerna run --stream build",
    "start": "yarn lerna run --parallel start"
  }
}
